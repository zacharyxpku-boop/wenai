import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '反向意图扩客 · 跳出固有客群标签 — Wenai',
  description:
    '阿里万相同款方法论: 美甲师→电子锁。AI 跳出固有标签,挖掘非显然但高转化的目标人群 + 卖点翻译 + 触达渠道。',
};

export default function IntentMiningLayout({ children }: { children: React.ReactNode }) {
  return children;
}
